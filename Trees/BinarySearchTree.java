public class BinarySearchTree{

    private class Node{

	private int value, count;
	private Node left, right; //children

    }

    private Node root;

    /*
current=root;
while(current!=null)
if (target<current.value)
current=current.getLeft()
>
=.getRight
if(==)
return [[find]] OR increment[[insert]]

toString
recursion, print out left <-- this --> right, and if anything == null print ' ' (a space)

     */

}
